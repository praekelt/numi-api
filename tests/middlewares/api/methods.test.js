describe("middleware/api/methods", () => {
  describe("create", () => {
    it("should validate the body");
    it("should set defaults on the body");
  });

  describe("update", () => {
    it("should ignore read only fields given in the body");
    it("should validate the body");
    it("should set defaults on the body");
  });

  describe("patch", () => {
    it("should validate the body");
    it("should set defaults on the body");
  });
});
