properties:
  id:
    type: string
    readOnly: true
    description: |
      The releases's identifier (generated by the API)

  url:
    type: string
    readOnly: true
    description: |
      API url for accessing the release's description.

  message:
    type: string
    readOnly: true
    description: |
      A human-readable message describing the release.

  dialogue:
    $ref: '#/definitions/dialogue-summary'
    readOnly: true
    description: |
      Summary of the dialogue associated with this release.

additionalProperties: false
