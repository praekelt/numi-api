allOf:
  - $ref: '#/definitions/user/user'

  - properties:
      password:
        type: string
        description: |
          The new user's password.

    required:
      - password

    additionalProperties: false
