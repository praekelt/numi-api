properties:
  type:
    enum:
      - revert

  properties:
    properties:
      revision_id:
        type: string
        description: |
          Identifier of the revision to revert to.
    required:
      - revision_id

required:
  - type
  - properties
