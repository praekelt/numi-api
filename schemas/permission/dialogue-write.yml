properties:
  type:
    enum:
      - dialogue:write

  properties:
    type: object
    properties:
      project_id:
        type: string
        description: |
          Identifier of the project this permission is for.

      dialogue_id:
        type: string
        description: |
          Identifier of the dialogue this permission is for.
    required:
      - project_id
      - dialogue_id

required:
  - type
  - properties
