type: object

properties:
  type:
    enum:
      - project:admin

  properties:
    type: object
    properties:
      project_id:
        type: string
        description: |
          Identifier of the project this permission is for.

    required:
      - project_id

required:
  - type
  - properties
