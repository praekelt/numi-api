$ref: '#/definitions/permission/permission'

properties:
  properties:
    type: object
    properties:
      project_id:
        type: string
        description: |
          Identifier of the project this permission is for.
