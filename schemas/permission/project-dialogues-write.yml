properties:
  type:
    enum:
      - project:dialogues:write

  properties:
    type: object
    properties:
      project_id:
        type: string
        description: |
          Identifier of the project this permission is for.

