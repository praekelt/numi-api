properties:
  id:
    type: string
    readOnly: true
    description: |
      The releases's identifier (generated by the API)

  url:
    type: string
    readOnly: true
    description: |
      API url for accessing the release's description.

  message:
    type: string
    description: |
      A human-readable message describing the release.

  dialogue:
    $ref: '#/definitions/dialogue'
    readOnly: true
    description: |
      The dialogue description associated with this release.

additionalProperties: false
