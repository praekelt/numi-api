properties:
  id:
    type: string
    readOnly: true
    description: |
      The releases's identifier (generated by the API)

  dialogue_id:
    type: string
    readOnly: true
    description: |
      The identifier for the dialogue associated with this release.

  url:
    type: string
    readOnly: true
    description: |
      API url for accessing the release's description.

  message:
    type: string
    description: |
      A human-readable message describing the release.

additionalProperties: false
