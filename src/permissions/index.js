const permission = require('src/permissions/permission');
const projects = require('src/permissions/projects');
const providers = require('src/permissions/providers');
const channels = require('src/permissions/channels');
const dialogues = require('src/permissions/dialogues');
const releases = require('src/permissions/releases');
const permissions = require('src/permissions/permissions');


module.exports = {
  permission,
  projects,
  dialogues,
  releases,
  providers,
  channels,
  permissions
};
